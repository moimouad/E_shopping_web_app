server:
  context-path: /
  port: 8013

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/test
    username: root
    password: 
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
